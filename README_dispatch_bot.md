# Бот рассылки заказов водителям (TK_Gorod_Bot) — деплой

Это MVP-скелет Части 2: бот принимает вебхук от parsing-bot о новой заявке,
разбирает тариф через GPT, шлёт превью reply на заявку в группу логистов
с кнопками `✅ Верно` / `✏️ Исправить`, по подтверждению пишет 9 тарифных
столбцов в "Orders clean".

Рассылка карточек заказов водителям с кнопками прогресса, счётчик времени
и остальное — следующие шаги, в этот файл ещё не входят.

---

## Подпись менеджера/автора в превью тарифа (09.08.2026)

Превью тарифа теперь начинается с тега того, кто ведёт заказ:
1. Сначала ищем строку "Менеджер .../ Логіст ..." прямо в тексте заявки
   (если там есть @username - берём именно его)
2. Если такой строки нет - берём автора сообщения из колонки
   "Автор_заявки" (её пишет parsing-bot, см. его README) - это тот, кто
   физически прислал заявку в группу

Пример:
```
@TK_Gorod
📋 Тариф по заказу
Авто: 1890 (фикса)
Ком. 300 грн

[ ✅ Верно ]  [ ✏️ Исправить ]
```

Если ни строки в тексте, ни колонки/значения в ней нет - строка с тегом
просто не показывается, превью начинается сразу с "📋 Тариф по заказу".

## Что уже сделано, что нет

**Есть:**
- Приём вебхука `/new_order` от parsing-bot
- Резолв 9 тарифных колонок по заголовку (не по номеру)
- GPT-разбор тарифа в JSON, превью логисту, запись после подтверждения
- Ограничение по группам: работает только в 3 группах логистов
  (`LOGIST_CHAT_IDS`), в группах водителей пока молчит

**Пока нет (следующие шаги):**
- Автоприменение правки тарифа из текста логиста к столбцам (сейчас
  правка только логируется, см. ниже, но не применяется автоматически)
- Fallback-опрос на случай, если вебхук от parsing-bot не долетел вообще
  (то есть превью не отправилось ни разу) — сейчас это единственный
  сценарий потери заказа, требующий ручной проверки

## Логирование исправлений тарифа (09.08.2026)

Когда логист жмёт `✏️ Исправить` и присылает верный тариф текстом (реплаем
на сообщение превью), бот пока НЕ пытается его разобрать и применить — это
отдельная будущая задача. Вместо этого он логирует пару "как разобрал
GPT" + "как исправил логист" в новый лист таблицы `Tariff_corrections`
(создаётся автоматически при первом обращении, как и `Log`).

Это сырьё для будущего улучшения точности разбора: когда наберётся
достаточно характерных случаев, самые показательные можно будет вручную
добавить как few-shot примеры прямо в промпт GPT (`_TARIFF_SYSTEM_PROMPT`)
— по тому же принципу, что уже работает с растущим тестовым корпусом
`geocode_test.py` в parsing-bot. Сам API OpenAI ничего не запоминает
между вызовами — обучения "на лету" не происходит, весь механизм
улучшения точности строится через промпт вручную.

Новая переменная окружения (необязательная):

| Имя переменной | Значение |
|---|---|
| `TARIFF_CORRECTIONS_TAB_NAME` | Название листа для лога исправлений. По умолчанию `Tariff_corrections` — можно не задавать. |

**Страховка от рестарта Railway между превью и нажатием кнопки** уже
встроена: `_pending_tariffs` живёт в памяти процесса, но если бот
перезапустится до того, как вы нажали `✅`/`✏️`, `get_or_reparse_tariff()`
просто заново прогонит текст заявки через GPT (детерминированно,
`temperature=0`) вместо ошибки "не нашёл в памяти" — нажатие кнопки
сработает как обычно, просто с небольшой задержкой на пересчёт.

---

## Часть 1. Токен TK_Gorod_Bot

Токен уже есть (бот создан заранее через `@BotFather`, добавлен в 3 группы
логистов + все группы водителей). Скопируйте токен из BotFather —
понадобится в Части 3.

Проверено и уже настроено:
- Group Privacy — выключен (бот видит все сообщения в группах, не только
  реплаи/команды)
- В группах логистов — обычный участник с доступом к сообщениям

---

## Часть 2. Выкладываем код на GitHub

1. На github.com → **+** → **New repository**
2. Имя, например `dispatch-bot`, **Private**, **Create repository**
3. **uploading an existing file** → загрузить `dispatch_bot.py`,
   `requirements.txt`
4. Добавить `Procfile` с содержимым:
   ```
   worker: python dispatch_bot.py
   ```
   (можно создать прямо на GitHub через **Add file → Create new file**)
5. **Commit changes**

---

## Часть 3. Разворачиваем на Railway

1. [railway.com](https://railway.com) → **New Project** →
   **Deploy from GitHub repo** → выбрать `dispatch-bot`
2. Первая сборка упадёт — переменные ещё не заданы, это нормально

### 3.1. Переменные окружения

Открыть сервис → **Variables** → **New Variable**:

| Имя переменной | Значение |
|---|---|
| `BOT_TOKEN` | токен TK_Gorod_Bot |
| `GOOGLE_SHEET_ID` | тот же ID таблицы, что в остальных ботах (скопировать из Variables любого существующего сервиса) |
| `GOOGLE_CREDENTIALS_JSON` | тот же JSON целиком, что и в остальных ботах (скопировать из Variables) |
| `SHEET_TAB_NAME` | `Orders clean` |
| `LOG_SHEET_TAB_NAME` | `Log` |
| `OPENAI_API_KEY` | тот же ключ, что использует parsing-bot |

**Важно:** Railway сам назначает переменную `PORT` для сервисов с
публичным доступом — трогать её не нужно, код читает её автоматически
(`os.environ.get("PORT", "8080")`).

### 3.2. Публичный URL для вебхука

Чтобы parsing-bot мог достучаться до этого бота, сервису нужен публичный
домен:

1. В сервисе `dispatch-bot` на Railway → **Settings** → **Networking** →
   **Generate Domain**
2. Скопировать полученный URL (вида
   `https://dispatch-bot-production.up.railway.app`)
3. В сервисе **parsing-bot** → **Variables** → добавить/обновить:
   `DISPATCH_BOT_WEBHOOK_URL` = `<этот URL>/new_order`
4. Передеплоить parsing-bot, чтобы переменная подхватилась

### 3.3. Проверка

**Deployments** → последний деплой → **View Logs**. Должна быть строка:
```
Вебхук-сервер запущен на порту ...
Бот рассылки запущен, начинаю polling...
```

---

## Часть 4. Тест

1. В одной из 3 групп логистов (например "Монитор логистов" — сейчас
   туда идут все заказы Кужеля для теста) написать тестовую заявку с
   тарифом, например:
   ```
   13.08 на 9:00
   Т1 Тестова 1
   Т2 Тестова 2
   5т +2 вантажника
   Тариф 5300/1200
   Ком 10%
   ```
2. parsing-bot должен обработать заявку как обычно (адреса, координаты)
3. dispatch-bot должен прислать превью тарифа **reply на это же
   сообщение**, примерно:
   ```
   📋 Тариф по заказу
   Авто: 5300 (0ч)/1200
   Ком. 10%
   ⚠️ Форма оплаты: не указана

   [ ✅ Верно ]  [ ✏️ Исправить ]
   ```
   (в тесте `min_chasov` не был указан явно, поэтому "0ч" — если хотите
   проверить с минимальными часами, добавьте в текст "мін 3год" или
   аналогично)
4. Нажать `✅ Верно` → в "Orders clean" в строке заявки должны
   заполниться 9 тарифных столбцов, сообщение должно поменяться на
   "✅ Подтверждено"
5. Проверить `Log` — должны появиться записи "Превью тарифа отправлено"
   и "Тариф подтверждён и записан"

Если шаг 4 сработал — можно переходить к следующему шагу (флоу
`✏️ Исправить` и/или карточки заказов водителям).
